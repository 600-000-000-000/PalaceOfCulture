# @600b/shared

TypeScript types shared by client and server: domain events, the asset model, state-machine
definitions, and conflict-resolution rules. Imported by `@600b/web` and `@600b/server` so
verification and vocabulary are identical on both sides.

No heavy runtime logic — types and pure helpers only. See `BUILD-BRIEF.md` §3.
