#!/usr/bin/env node
// CLI entrypoint for the asset pipeline. Stub — wiring lands with first-sprint backlog item 3.
import { PIPELINE_VERSION } from "./index";

// eslint-disable-next-line no-console
console.log(`600b-assets ${PIPELINE_VERSION} — pipeline not implemented yet (BUILD-BRIEF §6.3)`);
